{
  "mcpServers": {
    "hub-waoup": {
      "type": "http",
      "url": "${user_config.hub_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.hub_jeton}"
      }
    }
  }
}
